pseudo code for skeleton hunt

goal of the game is to complete the skeleton
players will take turns
one round each
set on a timer

6 body parts
60 seconds(this will vary...parameters set off the top of my head)

OPENING PAGE
players on screen...modal for rules and instructions on top left corner (modal...open and close..text inside)

make a start button and class
make player input fields...and make the input boxes disappear.

=======================================================================
click start button(this will make everything clear  and open the next window)


box on bottom for player name

box on bottom for timer

container on top left that has images running.

if( $(event.currentTarget).hasClass('skull2')){
const $skullfound = $('<img>').attr("src", "/Users/hectorguevara/Desktop/dev/Skeleton-Hunt/images/skull.png")
  $(".skeleton").append($skullfound);
  $($skull).removeClass("clickable")
}
else if ( $(event.currentTarget).hasClass('torso2')){
  const $torsofound = $('<img>').attr("src", "/Users/hectorguevara/Desktop/dev/Skeleton-Hunt/images/torso.png")
  $(".skeleton").append($torsofound);
  $($torso).removeClass("clickable");
}
}
